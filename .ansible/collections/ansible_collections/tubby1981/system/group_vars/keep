# group_vars

This directory is reserved for Ansible group variables.

⚠️ Do not commit real `*.yml` variable files here.  
Create your own local files such as `all.yml` or `webservers.yml` on your system.
